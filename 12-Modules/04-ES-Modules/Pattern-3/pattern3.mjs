// Pattern  - 3
// Summation
const add = function(x, y) {
  return x + y;
}

//Substract
const minus = function(x, y) {
  return y - x;
}

export default {
  add: add, // can only write key if key and value are same
  minus: minus
};