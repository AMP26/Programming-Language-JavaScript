// Pattern  - 3
// Summation
const add = function(x, y) {
  return x + y;
}

// Substract
const minus = function(x, y) {
  return y - x;
}

module.exports = {
  add: add, // can only write key if key and value is same
  minus: minus
};